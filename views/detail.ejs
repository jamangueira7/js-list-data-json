<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Exemplo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

</head>
<body>

    <a href="/" class="list-group-item list-group-item-action">Home</a>
<br>
<br>
<br>
    <div class="container">

        <form>
            <div class="mb-3">
                <label for="exampleInputEmail1" class="form-label">Dados de <%= user.name %></label>
            </div>
            <div class="mb-3">
                <select class="form-select
                form-select-lg mb-3"
                aria-label=".form-select-lg example"
                id="pais"
                name="pais"
                >
                  <option selected>Escolha</option>
                  <option value="<%= user.father %>">Pai</option>
                  <option value="<%= user.mother %>">Mãe</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="exampleInputEmail1" class="form-label">Nome</label>
                <input type="email" class="form-control" name="pai" id="pai" aria-describedby="emailHelp">
            </div>
        </form>
    </div>
    <script>
        $("#pais").change(function(){
          const result = document.querySelector('.pai');
          if($(this).val() == 'pai') {
            $("#pai").val($(this).val());
          } else {
            $("#pai").val($(this).val());
          }

        });
    </script>
</body>
</html>
